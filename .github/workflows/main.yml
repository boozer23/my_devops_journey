name: My First Pipeline

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Run Python
        run: python3 --version
        
      - name: Say Hello
        run: echo "Pipeline работает!"